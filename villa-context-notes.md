# 비트별장 예약 기능 — 컨텍스트 노트

작업 중 내린 결정과 그 이유를 계속 append한다. 다음 세션이 판단을 재도출하지 않고 이어받기 위한 문서다.

---

## 2026-07-27 — 기획 단계

### 사용자에게 확인받은 4가지 (착수 전)

데이터 모델과 보안 설계를 좌우하는 항목이라 가정하지 않고 물었다.

| 질문 | 답 | 왜 물었나 |
|---|---|---|
| 이용 기간 방식 | 체크인~체크아웃 날짜 범위 (연박 가능) | 단일 날짜 vs 범위는 모델·달력·충돌 판정을 전부 바꾼다. "이용날짜"라는 단수 표현과 "예상 입퇴실 시간"이라는 시각 표현이 섞여 있어 양쪽 해석이 가능했다 |
| 정규예약 주기 | 대상월 2개월 전 1일~말일 (예시 그대로) | 주신 예시(10월 → 8/1~8/31)가 2개월 전으로 읽혀 오타 가능성을 배제해야 했다 |
| SMTP 비밀번호 | DB에 암호화 저장, 키는 `.env` | `SystemSetting.value`가 평문 `Text`다. 이번 세션에서 방금 제거한 것과 같은 종류의 비밀 노출을 다시 만들 수 있었다 |
| 추가입력 페이지 인증 | SSO 로그인 필수 + 본인 예약만 | 메일 링크는 전달될 수 있다. 차량번호는 개인정보라 토큰만으로는 부족하다고 판단했고 사용자도 동의했다 |

### 설계 결정과 이유

**결정 1. 별장 엔티티는 `Facility` 재사용, 예약은 새 테이블.**

`Facility`(type='villa')로 별장 2개를 시드한다. gym/golf와 성격이 같고 `initial_data.py` 시드 패턴을 그대로 쓸 수 있다.

반대로 예약은 기존 `Reservation`을 재사용하지 **않는다.** 생애주기가 근본적으로 다르다.
- 골프: 즉시 확정 + 우선순위 선점
- 별장: 신청 → 중복 경합 → 관리자 확정 → 추가입력

`Reservation`에 얹으면 `priority`가 무의미해지고, 차량·이용구성 컬럼이 골프 행에서는 전부 NULL이 되고, `status` 어휘가 두 도메인에 걸쳐 뒤섞인다. 별도 테이블이 맞다.

→ **부작용 하나 확인함:** [admin.py:57](backend/app/routers/admin.py:57)의 `"Health" if type == "gym" else "Screen Golf"`는 villa를 "Screen Golf"로 표기한다. 별장은 `AccessLog`를 쓰지 않아 현재는 도달 불가지만, 향후 별장에 QR 입퇴실을 붙이면 이 분기를 먼저 고쳐야 한다. 계획서 3-1절에 경고로 남겼다.

**결정 2. 체크아웃 날짜는 배타적으로 다룬다.**

`8/1~8/3`(2박)과 `8/3~8/5`(2박)는 겹치지 않는다. 8/3에 한 팀이 퇴실하고 다른 팀이 입실한다. 겹침 조건은 `a.start < b.end and a.end > b.start`로, **골프 슬롯 겹침 로직과 완전히 같은 패턴**이다([golf.py:129](backend/app/routers/golf.py:129), [golf.py:236](backend/app/routers/golf.py:236)).

→ 기존 패턴을 그대로 쓰는 게 중요하다. 별장만 다른 규칙을 쓰면 나중에 둘 중 하나를 고칠 때 실수한다. Phase 2 테스트에서 이 경계를 반드시 고정한다.

**결정 3. `Date` 타입을 쓴다.**

기존 모델은 전부 `DateTime`이지만 별장은 날짜 단위라 `Date`가 맞다. 부수 효과로 이번 세션 Phase 5에서 고생한 tz-aware/naive 혼용 문제가 **구조적으로 재발할 수 없다**. `created_at` 같은 시각 컬럼은 Phase 5 정책대로 KST naive(`datetime.now`)를 쓴다.

**결정 4. 암호화 키 부재 시 동작을 `SECRET_KEY`와 다르게 설계한다.**

`SECRET_KEY`는 없으면 `RuntimeError`로 앱 기동을 막는다(Phase 3). 조용히 취약한 키를 쓰는 것보다 안전하기 때문이다.

`SETTINGS_ENCRYPTION_KEY`는 그렇게 하면 **안 된다.** SMTP는 부가 기능인데 키가 없다고 헬스·골프·별장 예약까지 다 죽으면 가용성 손해가 보안 이득보다 크다. 따라서 모듈 import 시점이 아니라 **암복호화 호출 시점에** 명확한 에러를 던진다. 메일 기능만 비활성된다.

→ 두 키의 실패 정책이 다른 이유를 여기 남긴다. 나중에 "일관성"을 이유로 통일하려는 시도가 있을 수 있는데, 의도적인 차이다.

**결정 5. 캘린더 라이브러리를 도입하지 않는다.**

월 그리드는 순수 `Date` 연산 80줄 수준이고, 기존 프론트엔드는 날짜 라이브러리 없이 동작한다(`date-fns`, `dayjs` 모두 미설치). 직전 대화에서 검토한 shadcn/ui도 이 기능에는 도입하지 않는다 — 별건 판단이고, 지금 Tailwind 커스텀 톤을 유지하는 게 UI 일관성에 낫다.

**결정 6. 관리자 확정 화면에 과거 이용 이력을 노출한다.**

요구사항은 "관리자가 선택해 확정"까지만 정했다. 하지만 관리자가 무엇을 보고 고를지가 이 화면의 본질이다. 신청 순서만 보여주면 사실상 선착순이 되어 정규예약의 취지(공정 배분)가 사라진다. 그래서 신청자·부서·인원·신청 순서에 **과거 이용 횟수**를 더해 보여준다.

→ 이건 내가 제안한 부분이다. 공정성 규칙 자체(예: 연 2회 제한)를 코드로 강제하지는 않는다 — 요구사항에 없고, 관리자 재량을 남기는 쪽이 안전하다.

**결정 7. 스케줄러 작업에 중복 실행 방지 플래그를 필수로 둔다.**

[main.py:111](backend/app/main.py:111)의 `scheduled_jobs()`는 `CronTrigger(minute='*')`로 **1분마다** 돈다. 여기에 "매월 1일 회차 생성", "통보일 발송" 같은 날짜성 작업을 넣으면 하루에 1440번 실행된다.

기존 코드도 같은 문제를 `notified_slack` 플래그로 해결하고 있다([main.py:77](backend/app/main.py:77)). 같은 방식으로 `round.status`와 `notified_confirmed`를 게이트로 쓴다.

→ Phase 7에서 가장 실수하기 쉬운 지점이다. 테스트로 반드시 고정한다.

### 가정으로 진행하는 것 (계획서에 명시함)

사용자 확인 없이 기본안을 정했다. 이견이 있으면 해당 Phase에서 조정한다.

1. **선착순은 즉시 확정** — "선착순"의 자연스러운 해석이고, 관리자 개입 없이 빈 날짜를 채우는 게 요구사항 취지로 보인다. 다르다면 Phase 7에서만 바꾸면 되고 앞 단계에 영향 없다.
2. **성인+아동 합계 ≠ 사용인원이어도 저장 허용** — 확정 후 동행 인원이 바뀌는 건 자연스럽다. 강제 일치는 사용자를 막는다. 대신 관리자 목록에 불일치 표시를 남긴다.
3. **통보일에 미확정 경합이 남으면 통보 보류 + 관리자 경고** — 임의 자동 선정은 하지 않는다. 사람이 정할 일을 시스템이 대신 정하면 분쟁이 생긴다.
4. **별장 1채는 한 시점에 한 팀 독점** — 휴양소 성격상 자연스럽다. `capacity`는 인원 상한 검증에만 쓴다.

### 답이 필요한 열린 항목

Phase 2 착수 전에 1·6·7번은 반드시 확정해야 한다. 나머지는 해당 Phase 전까지 받으면 된다.

1. 확정 후 취소 정책 — 선착순으로 재개방할지, 관리자 승인을 거칠지
2. 이번 달 출석 중복 표시 — [page.tsx:108](frontend/app/page.tsx:108)과 [page.tsx:156](frontend/app/page.tsx:156)에 같은 수치가 두 번 나온다. 메뉴 재배치하면서 정리할지
6. **실제 정원** — 계획서의 청평 12명 / 동비재 10명은 임의 예시값이다
7. **연박 상한** — 최대 몇 박까지

### 다음 세션이 알아야 할 것

1. 겹침 판정은 골프와 **같은 패턴**을 쓴다(결정 2). 별장만 다르게 짜지 말 것.
2. `SETTINGS_ENCRYPTION_KEY`와 `SECRET_KEY`의 실패 정책이 다른 것은 **의도적**이다(결정 4).
3. 스케줄러는 1분마다 돈다. 날짜성 작업에 플래그 없이 넣으면 1440번 실행된다(결정 7).
4. 별장 정원(12/10)은 아직 **확인되지 않은 임의값**이다. 시드 전에 실제 값을 받아야 한다.
5. 기존 테스트 93개가 통과 중이다. `conftest.py`의 `facilities` 픽스처를 수정할 때 기존 gym/golf 테스트가 깨지지 않는지 확인할 것.
